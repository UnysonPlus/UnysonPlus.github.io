{
  "_comment": "SOURCE OF TRUTH for the Theme Settings mapping docs. Hand-curated from the Site Converter's design-system extraction (class-fw-site-converter-stitch.php build_color_presets / typography / button logic + class-fw-site-converter-theme-settings.php importer) cross-referenced against the theme's option groups (unysonplus-theme/framework-customizations/theme/options/*.php, documented in theme-settings-reference.md). Edit THIS file, then run `node scripts/gen-theme-settings-mapping.mjs` to regenerate ai-dev-kit/theme-settings-mapping/*.md. Do NOT hand-edit the generated .md. Option 'status': native = derived from a source signal; via-css = reproduced via generated CSS, native option empty; unmapped = left at default (no source signal / manual); auto = plumbing. Tab 'coverage': populated = the converter writes this tab; partial = only some keys; manual = not converter-populated (set by hand or by the generated child theme).",
  "sourceOfTruth": "unysonplus/framework/extensions/site-converter — stitch.php (design-system extraction) + theme-settings.php (importer); options from unysonplus-theme/framework-customizations/theme/options/*.php",
  "statuses": {
    "native": {
      "emoji": "✅",
      "label": "Native",
      "blurb": "Derived and written from a source signal."
    },
    "via-css": {
      "emoji": "🟡",
      "label": "Via CSS",
      "blurb": "Reproduced via generated CSS; the native option is left empty (candidate to promote)."
    },
    "unmapped": {
      "emoji": "⚪",
      "label": "Unmapped",
      "blurb": "Left at default — no source signal, or set by hand."
    },
    "auto": {
      "emoji": "⚙️",
      "label": "Auto",
      "blurb": "Plumbing. Excluded from the coverage percentage."
    }
  },
  "tabCoverage": {
    "populated": {
      "emoji": "✅",
      "label": "Populated"
    },
    "partial": {
      "emoji": "🟡",
      "label": "Partial"
    },
    "manual": {
      "emoji": "⚪",
      "label": "Manual"
    }
  },
  "tabs": [
    {
      "group": "General",
      "tab": "Colors",
      "slug": "colors",
      "storageKey": "theme_colors",
      "location": "Theme Settings → General → Colors",
      "coverage": "populated",
      "intro": "The Colors tab is the site **palette** — the `theme_colors` addable list of `{ name, color }` rows. Each row's name becomes a slug that drives a `.text-{slug}` / `.bg-{slug}` utility and a `--color-{slug}` CSS variable (e.g. **Primary** drives `btn-primary` + `--color-primary`). The converter derives the source's **brand palette** and writes these rows, then merges the plugin's default presets after them (de-duped by hex) so bundled Box / Icon-Badge presets that reference default slugs still resolve.",
      "recognizerNotes": "Brand colours are pulled from the captured design tokens (a real `tailwind.config`) first, then from each element's computed `getComputedStyle` semantic colours, then a markup accent scan / a gradient-accent scan / the primary button's real fill; body text falls back to a sampled ink colour. A brand role only appears when a real colour was found — otherwise the default palette stands. The brand `Primary` is prepended before the default `Primary`, so it wins the `primary` slug and the default button/box presets adopt the brand colour.",
      "options": [
        {
          "key": "Primary",
          "type": "palette role",
          "status": "native",
          "source": "Brand action colour, in priority order: `tailwind.config` accent/primary token → computed semantic primary/accent → vivid markup accent scan → the primary button's real fill → tertiary/secondary/ink → neutral fallback. Wins the `primary` slug (drives `btn-primary` + `--color-primary`)."
        },
        {
          "key": "Ink",
          "type": "palette role",
          "status": "native",
          "source": "Source text / foreground colour (`text`, `on-background`, `foreground`), or — when no token exists — **sampled from the real rendered body-copy colour** (`sample_ink`). Falls back to `#1a1a1a`."
        },
        {
          "key": "Secondary",
          "type": "palette role",
          "status": "native",
          "source": "`secondary` / `tertiary` token, or the computed semantic `secondary`. Only when found."
        },
        {
          "key": "Accent",
          "type": "palette role",
          "status": "native",
          "source": "Computed semantic `accent`, or — when the design has no named accent — an accent colour **lifted from a source gradient** (`scan_gradient_accent`, e.g. the vivid stop in a hero gradient). Only when found."
        },
        {
          "key": "Dark",
          "type": "palette role",
          "status": "native",
          "source": "`deep-black` / `black` / `surface-container-lowest` token, or semantic `dark`. Only when found."
        },
        {
          "key": "Muted",
          "type": "palette role",
          "status": "native",
          "source": "`muted` / `on-surface-variant`. Only when found."
        },
        {
          "key": "Light",
          "type": "palette role",
          "status": "native",
          "source": "`page-bg` / `background` / `surface` / `white-soft`. Only when found."
        },
        {
          "key": "Default presets (White, Gray, Red, Green, …)",
          "type": "palette rows",
          "status": "unmapped",
          "source": "The plugin's ~20 default color presets, merged in after the brand roles and de-duped by hex — kept (not derived) so bundled Box / Icon-Badge presets that reference their slugs keep resolving."
        }
      ]
    },
    {
      "group": "Design System",
      "tab": "Components",
      "slug": "components",
      "storageKey": "button_presets · box_presets · text_styles · theme_colors · image_styles · section_style_presets · …",
      "location": "Theme Settings → Components",
      "coverage": "populated",
      "intro": "Components is where the converter reproduces the source’s **design system** most heavily. It’s a single Theme Settings tab, but it holds many **preset libraries** — buttons, boxes, text styles, colours, badges, image styles, section skins, spacing — and the converter **derives almost all of them** from the captured DOM + computed styles, writing each as a fully-editable **addable-box** (not baked CSS).",
      "recognizerNotes": "These presets are what the converted **elements** then consume — a button points at a derived Button preset, a card at a derived Box preset, a heading at a derived Text Style — so editing one preset restyles every element that uses it, site-wide. Each library below has its own page here (what the converter fills) and a full reference under **[The Theme → Components](/theme/components)** (how it’s coded + examples).",
      "presets": [
        {
          "name": "Buttons",
          "slug": "buttons",
          "ref": "/theme/components/buttons",
          "coverage": "populated",
          "storageKey": "button_colors · button_sizes · button_animations",
          "optionFile": "framework/extensions/shortcodes/includes/theme-settings/components-buttons.php (schema in framework/includes/option-types/button-presets/)",
          "builderFn": "FW_Site_Converter_Stitch::build_button_presets()",
          "outputClass": "a `.btn-{slug}` class per preset (e.g. `.btn-primary`) + a `.btn-{size}` per size",
          "intro": "The Buttons library holds three axes: **Button Presets** (the *skin* — colour, gradient, border, shadow, font, per state), **Sizes** (the *dimensions* — font-size, padding, radius, height), and **Hover Animations** (CSS effects). The converter reads the source’s real buttons and rebuilds the skin + size axes; each preset renders a live preview and produces a `.btn-{slug}` class the Button shortcode points at.",
          "wiringNote": "The skin axis carries **colour-family properties only** (bg / text / border / shadow / transform); dimensional properties (font-size, padding, radius, height) deliberately live on the **Sizes** axis, so a colour and a size compose independently. The converter clusters the source’s button variants into named presets (Primary / Secondary / Accent…) and its distinct sizes into S / M / L.",
          "notes": ":::note[How the states work]\nEach preset has **five state tabs** — **Default / Hover / Active / Focus / Disabled** — sharing one `transition` and one `{{SELECTOR}}`-aware Custom CSS block. The converter fills **Default** fully and **Hover** partially (bg / text / border); **Active / Focus / Disabled** are left empty so they inherit Default, and you set them by hand only if the source distinguishes them. A source button that **animates on hover** (e.g. a transform) gets a matching `transition: transform …` rule injected into **Custom CSS** rather than the `transition` field.\n:::\n\n:::info[Gradients]\nA source button background that’s a **gradient** is flattened into the `bg_color` fill (for hover, to an `rgba()`), so the native `gradient` field is left blank — the button still looks right, and you can promote it back to a real gradient by hand.\n:::",
          "options": [
            {
              "group": "Preset · Identity (`shared_top`)",
              "key": "color_name",
              "type": "text",
              "status": "native",
              "source": "The preset name — clustered from the source’s button variants (Primary / Secondary / Accent…)"
            },
            {
              "group": "Preset · Identity (`shared_top`)",
              "key": "slug",
              "type": "unique",
              "status": "auto",
              "source": "Auto from the name (`sanitize_title_with_dashes`) → the `.btn-{slug}` class suffix"
            },
            {
              "group": "Preset · Identity (`shared_top`)",
              "key": "font",
              "type": "typography",
              "status": "native",
              "source": "The button font — **identity only** (family / variation / script / letter-spacing). Carried when it **deviates** from the body base; otherwise left to inherit"
            },
            {
              "group": "Preset · State: Default",
              "key": "bg_color",
              "type": "compact color",
              "status": "native",
              "source": "The button’s computed background fill"
            },
            {
              "group": "Preset · State: Default",
              "key": "text_color",
              "type": "compact color",
              "status": "native",
              "source": "The button’s computed text colour"
            },
            {
              "group": "Preset · State: Default",
              "key": "gradient",
              "type": "gradient-v2",
              "status": "unmapped",
              "source": "Gradients are flattened into `bg_color`; this field is left blank"
            },
            {
              "group": "Preset · State: Default",
              "key": "text_transform",
              "type": "short-select",
              "status": "native",
              "source": "e.g. `uppercase` — carried so Theme Settings shows the real casing (not baked into CSS)"
            },
            {
              "group": "Preset · State: Default",
              "key": "border_style",
              "type": "select",
              "status": "native",
              "source": "Carried when the source button has a border"
            },
            {
              "group": "Preset · State: Default",
              "key": "border_color",
              "type": "compact color",
              "status": "native",
              "source": "Carried when the source button has a border"
            },
            {
              "group": "Preset · State: Default",
              "key": "border_width",
              "type": "unit-input",
              "status": "native",
              "source": "Carried when the source button has a border"
            },
            {
              "group": "Preset · State: Default",
              "key": "box_shadow",
              "type": "box-shadow (X/Y/blur/spread/color/inset)",
              "status": "native",
              "source": "Carried when the source button has a drop shadow"
            },
            {
              "group": "Preset · State: Hover",
              "key": "bg_color",
              "type": "compact color",
              "status": "native",
              "source": "The hover background (a gradient hover is flattened to an `rgba()` fill)"
            },
            {
              "group": "Preset · State: Hover",
              "key": "text_color",
              "type": "compact color",
              "status": "native",
              "source": "The hover text colour, when it changes"
            },
            {
              "group": "Preset · State: Hover",
              "key": "border_color",
              "type": "compact color",
              "status": "native",
              "source": "The hover border colour, when present"
            },
            {
              "group": "Preset · State: Hover",
              "key": "gradient · text_transform · border_* · box_shadow",
              "type": "—",
              "status": "unmapped",
              "source": "Left at the Default/theme value on hover"
            },
            {
              "group": "Preset · States: Active / Focus / Disabled",
              "key": "(all fields)",
              "type": "—",
              "status": "unmapped",
              "source": "Left empty → inherit Default. Set by hand only if the source distinguishes these states"
            },
            {
              "group": "Preset · Shared (`shared_bottom`)",
              "key": "transition",
              "type": "short-text (ms)",
              "status": "unmapped",
              "source": "Theme default — a transform ease is injected into Custom CSS instead when the source button animates"
            },
            {
              "group": "Preset · Shared (`shared_bottom`)",
              "key": "custom_css",
              "type": "code-editor",
              "status": "auto",
              "source": "The converter injects a hover-transform rule here when the source button animates on hover"
            },
            {
              "group": "Sizes (`button_sizes`)",
              "key": "size_name",
              "type": "text",
              "status": "native",
              "source": "Named from the clustered sizes (S / M / L, or the distinct sizes found)"
            },
            {
              "group": "Sizes (`button_sizes`)",
              "key": "slug",
              "type": "text",
              "status": "auto",
              "source": "The `.btn-{slug}` size-class suffix"
            },
            {
              "group": "Sizes (`button_sizes`)",
              "key": "font_size",
              "type": "unit-input",
              "status": "native",
              "source": "Per-size text size"
            },
            {
              "group": "Sizes (`button_sizes`)",
              "key": "line_height",
              "type": "short-text",
              "status": "unmapped",
              "source": "Left default"
            },
            {
              "group": "Sizes (`button_sizes`)",
              "key": "padding_y",
              "type": "unit-input",
              "status": "native",
              "source": "Top / bottom padding"
            },
            {
              "group": "Sizes (`button_sizes`)",
              "key": "padding_x",
              "type": "unit-input",
              "status": "native",
              "source": "Left / right padding"
            },
            {
              "group": "Sizes (`button_sizes`)",
              "key": "border_radius",
              "type": "unit-input",
              "status": "native",
              "source": "Corner radius (`9999px` = pill)"
            },
            {
              "group": "Sizes (`button_sizes`)",
              "key": "min_height",
              "type": "unit-input",
              "status": "native",
              "source": "A **fixed** source height (e.g. `h-11` = 44px) → Min Height; content centres to it via inline-flex"
            },
            {
              "group": "Sizes (`button_sizes`)",
              "key": "min_width · max_width",
              "type": "unit-input",
              "status": "unmapped",
              "source": "Left default"
            },
            {
              "group": "Hover Animations (`button_animations`)",
              "key": "name · css",
              "type": "addable-box + code-editor",
              "status": "unmapped",
              "source": "Not derived — the theme ships default hover animations (`{{BTN}}` / `{{ANIM}}`); add source-specific ones by hand"
            }
          ]
        },
        {
          "name": "Box Presets",
          "slug": "box-presets",
          "ref": "/theme/components/box-presets",
          "coverage": "populated",
          "storageKey": "border_presets",
          "optionFile": "framework/extensions/shortcodes/includes/theme-settings/components-box.php (schema in framework/includes/option-types/border-presets/)",
          "builderFn": "FW_Site_Converter_Stitch::build_box_presets()",
          "outputClass": "a `.boxp-{slug}` class per preset — pick it on a Column (Styling → Box Preset), a Table (Table Options → Frame), or a Countdown",
          "intro": "A Box Preset is a reusable **card skin** — background fill, border, corner radius, padding, box-shadow, and a hover treatment — that any Column, Table frame or Countdown can point at. The converter clusters the source’s repeated “card” looks into named presets and fills the fill + border + radius + shadow, plus the hover skin and hover effects.",
          "wiringNote": "Each preset carries a **Default** and a **Hover** state (the fill + border skin), shared geometry (corner radius, sides, padding), a transition, and a hover-effects list. The converter only makes a preset for a card that **deviates** from the page base; near-identical cards cluster into one, so you get a handful of named card styles instead of dozens of one-offs.",
          "notes": ":::note[Padding is intentionally left to the Column]\nThe converter leaves the preset’s **Padding** empty. A card’s inner padding is usually set by the Column it’s on (Styling → Margin & Padding), which overrides the preset — so filling it here would fight the column. The preset stays a pure *skin*.\n:::\n\n:::info[Tier-3 effects go to Custom CSS]\nA native `hover_fx` covers the common hovers — **`lift`** (a translate-Y) and **`glow`** (a shadow). Effects the native fields can’t express — a frosted `backdrop-filter`, or a hover **`scale`** — are injected into the preset’s **Custom CSS** instead, so the look is reproduced without a bespoke field.\n:::",
          "options": [
            {
              "group": "Preset · Identity (`shared_top`)",
              "key": "preset_name",
              "type": "text",
              "status": "native",
              "source": "The card name — clustered from the source’s repeated card looks (or `Box <hash>`) → the `.boxp-{slug}` class"
            },
            {
              "group": "Preset · Identity (`shared_top`)",
              "key": "slug",
              "type": "unique",
              "status": "auto",
              "source": "Auto from the name → the `.boxp-{slug}` class suffix"
            },
            {
              "group": "Preset · State: Default",
              "key": "background",
              "type": "background-pro (color / gradient / image)",
              "status": "native",
              "source": "The card’s background fill"
            },
            {
              "group": "Preset · State: Default",
              "key": "border_style",
              "type": "short-select",
              "status": "native",
              "source": "`solid` when the source card has a border"
            },
            {
              "group": "Preset · State: Default",
              "key": "border_width",
              "type": "unit-input",
              "status": "native",
              "source": "Carried when the source card has a border"
            },
            {
              "group": "Preset · State: Default",
              "key": "border_color",
              "type": "compact color",
              "status": "native",
              "source": "Carried when the source card has a border"
            },
            {
              "group": "Preset · State: Default",
              "key": "box_shadow",
              "type": "box-shadow (X/Y/blur/spread/color/inset)",
              "status": "native",
              "source": "Carried when the source card has a drop shadow"
            },
            {
              "group": "Preset · State: Hover",
              "key": "background",
              "type": "background-pro",
              "status": "native",
              "source": "The hover fill, when the card changes on hover"
            },
            {
              "group": "Preset · State: Hover",
              "key": "border_color",
              "type": "compact color",
              "status": "native",
              "source": "The hover border colour, when present"
            },
            {
              "group": "Preset · State: Hover",
              "key": "box_shadow",
              "type": "box-shadow",
              "status": "native",
              "source": "The hover shadow, when present"
            },
            {
              "group": "Preset · State: Hover",
              "key": "border_style · border_width",
              "type": "—",
              "status": "unmapped",
              "source": "Left at the Default value on hover"
            },
            {
              "group": "Preset · Shared (`shared_bottom`)",
              "key": "border_radius",
              "type": "unit-input",
              "status": "native",
              "source": "Corner radius from the source card"
            },
            {
              "group": "Preset · Shared (`shared_bottom`)",
              "key": "border_sides",
              "type": "short-select",
              "status": "auto",
              "source": "Always `all` — the converter doesn’t infer per-side borders"
            },
            {
              "group": "Preset · Shared (`shared_bottom`)",
              "key": "padding",
              "type": "spacing (per-side)",
              "status": "unmapped",
              "source": "Left empty — the Column’s own Margin & Padding controls it (see note)"
            },
            {
              "group": "Preset · Shared (`shared_bottom`)",
              "key": "transition",
              "type": "short-text (ms)",
              "status": "auto",
              "source": "Fixed at `200` ms"
            },
            {
              "group": "Preset · Shared (`shared_bottom`)",
              "key": "hover_fx",
              "type": "multi-select",
              "status": "native",
              "source": "Derived: `lift` (translate-Y) and/or `glow` (shadow) from the source’s hover"
            },
            {
              "group": "Preset · Shared (`shared_bottom`)",
              "key": "custom_css",
              "type": "code-editor ({{SELECTOR}})",
              "status": "auto",
              "source": "Tier-3 effects the native fields can’t express — a frosted `backdrop-filter`, a hover `scale` — are injected here"
            }
          ]
        },
        {
          "name": "Text Styles",
          "slug": "text-styles",
          "ref": "/theme/components/text-styles",
          "coverage": "populated",
          "storageKey": "font_sizes",
          "optionFile": "framework/extensions/shortcodes/includes/theme-settings/components-typography.php",
          "builderFn": "FW_Site_Converter_Stitch::build_text_styles()",
          "outputClass": "a `.font-{slug}` (or your literal Class) per style — offered in every shortcode’s Styling → Text Style dropdown",
          "intro": "A Text Style is a named, reusable typographic token — a size **plus** optional weight / line-height / letter-spacing / transform / colour, every field opt-in (a blank field inherits the element’s own tag). The converter distils the source’s heading + paragraph scale into a set of named styles — **Display 1…N**, **Lead**, body **buckets**, and an **Eyebrow** — each becoming a `.font-{slug}` utility. This is where the source’s type *scale* lands (not the Typography tab’s per-heading overrides).",
          "wiringNote": "The converter measures the computed styles (`data-sc-cs`) of the source’s real h1–h6 and paragraphs, clusters them by size (largest first), and emits the distinct roles: **Display 1…N** (the big headings), **Lead** (the intro paragraph), body-size **buckets**, and an **Eyebrow** (the small uppercase label → `.font-eyebrow`). Stored under the legacy `font_sizes` key for wiring compatibility.",
          "notes": ":::note[Every field is opt-in — blank inherits]\nA Text Style emits **only** the fields that are filled, scoped to its own class, so a blank property inherits from the element’s tag token (a blank weight is **not** thin — it keeps the heading’s weight). The converter follows the same rule: a measured weight of `400` is left **blank** (inherit), and a colour is carried **only** when it isn’t near-black/white — so a converted style overrides just what actually deviates from the source’s tag defaults.\n:::",
          "options": [
            {
              "group": "Style (`font_sizes` box)",
              "key": "name",
              "type": "text",
              "status": "native",
              "source": "The role name shown in the dropdown — Display 1…N / Lead / (body bucket) / Eyebrow"
            },
            {
              "group": "Style (`font_sizes` box)",
              "key": "size",
              "type": "unit-input",
              "status": "native",
              "source": "The measured size for that role (largest headings first)"
            },
            {
              "group": "Style (`font_sizes` box)",
              "key": "weight",
              "type": "select",
              "status": "native",
              "source": "Measured font-weight; `400` is left blank (inherit the tag)"
            },
            {
              "group": "Style (`font_sizes` box)",
              "key": "line_height",
              "type": "text",
              "status": "native",
              "source": "Measured line-height (unitless or length)"
            },
            {
              "group": "Style (`font_sizes` box)",
              "key": "letter_spacing",
              "type": "unit-input",
              "status": "native",
              "source": "Measured tracking (em / px)"
            },
            {
              "group": "Style (`font_sizes` box)",
              "key": "transform",
              "type": "select",
              "status": "native",
              "source": "Only the **Eyebrow** gets `uppercase`; other roles inherit"
            },
            {
              "group": "Style (`font_sizes` box)",
              "key": "color",
              "type": "compact color",
              "status": "native",
              "source": "Carried **only** when it isn’t near-black/white"
            },
            {
              "group": "Style (`font_sizes` box)",
              "key": "class",
              "type": "text",
              "status": "auto",
              "source": "Auto `.font-{slug}` (or `display-1` / `lead` / `font-eyebrow`) — the output utility class"
            },
            {
              "group": "Style (`font_sizes` box)",
              "key": "custom_css",
              "type": "textarea (`selector`)",
              "status": "unmapped",
              "source": "Not derived — add text-shadow / gradient-text etc. by hand"
            }
          ]
        },
        {
          "name": "Color Presets",
          "slug": "color-presets",
          "ref": "/theme/components/color-presets",
          "coverage": "populated",
          "storageKey": "theme_colors",
          "optionFile": "framework/extensions/shortcodes/includes/theme-settings/components-color.php",
          "builderFn": "FW_Site_Converter_Stitch::build_color_presets()",
          "outputClass": "`.text-{slug}` / `.bg-{slug}` classes and a `--color-{slug}` CSS variable per swatch",
          "intro": "Color Presets is the site **palette** — the swatches every Text Color / Background Color dropdown offers, and the colours the Button / Box / Table presets reference. The converter extracts the source’s **brand palette** and names each role (Primary / Secondary / Accent, plus Ink / Dark / Light), then merges in any non-colliding default swatches.",
          "wiringNote": "The converter reads the brand colours from the extracted design **tokens** and the **DOM** — including a distinct **Accent** lifted from hero gradients (e.g. the yellow half of a green→yellow gradient) so the Accent role isn’t defaulted to a stock colour the source never used. It assigns the role names, then prepends the default palette **minus** any default whose slug collides with a brand role — so there is exactly one Primary, one Secondary, and so on.",
          "notes": ":::note[Why the swatch value is “native” but the name is “auto”]\nThe **colour** of each swatch is a real signal — the brand hex pulled from the source. The **name** is assigned by the converter (Primary / Secondary / Accent / Ink …) rather than read from the source, so it’s marked *auto*. The slug that names the output class + CSS variable is derived from that name the same way `css-tokens.php` does, so `.text-primary` / `--color-primary` line up across the whole site.\n:::",
          "options": [
            {
              "group": "Swatch (`theme_colors` box)",
              "key": "name",
              "type": "text",
              "status": "auto",
              "source": "Role-named automatically — Primary / Secondary / Accent (from brand tokens + hero gradients) plus Ink / Dark / Light"
            },
            {
              "group": "Swatch (`theme_colors` box)",
              "key": "color",
              "type": "color-picker",
              "status": "native",
              "source": "The brand hex extracted from the source (design tokens + DOM)"
            }
          ]
        },
        {
          "name": "Icon Badges",
          "slug": "icon-badges",
          "ref": "/theme/components/icon-badges",
          "coverage": "populated",
          "storageKey": "icon_badge_presets",
          "optionFile": "framework/extensions/shortcodes/includes/theme-settings/components-icon-badges.php (schema in framework/includes/option-types/icon-badge-presets/)",
          "builderFn": "FW_Site_Converter_Stitch::build_icon_badge_presets()",
          "outputClass": "a `.iconb-{slug}` class per preset — pick it on an Icon Box (Styling → Icon Badge Preset)",
          "intro": "An Icon Badge is a reusable **shaped tile for an icon** — a circle / rounded / square / hexagon at a set size, with a tile fill, a centred glyph (its own colour + size), border and box-shadow. The converter detects the source’s icon-in-a-tile pattern, clusters the distinct tiles, and rebuilds the Default-state skin plus the geometry.",
          "wiringNote": "The converter finds the source’s icon-in-a-tile pattern and fills the **Default** state (tile fill, glyph colour, border) plus the geometry — **shape**, **tile size**, **icon size** (derived as ≈ 50% of the tile), and a **corner radius** for the Rounded shape (Circle is always 50%). The **Hover** state, box-shadow and hover effects are left at the theme default.",
          "notes": ":::note[Icon size follows the tile]\nThe converter sets the glyph **Icon Size** to about **half** the tile’s **Badge Size** (a common, safe ratio) rather than measuring the source glyph separately — so a 48px tile gets a 24px icon. Adjust it on the preset if the source uses a different ratio.\n:::",
          "options": [
            {
              "group": "Preset · Identity (`shared_top`)",
              "key": "preset_name",
              "type": "text",
              "status": "native",
              "source": "The badge name — from the clustered tile → the `.iconb-{slug}` class"
            },
            {
              "group": "Preset · Identity (`shared_top`)",
              "key": "slug",
              "type": "unique",
              "status": "auto",
              "source": "Auto from the name → the `.iconb-{slug}` class suffix"
            },
            {
              "group": "Preset · State: Default",
              "key": "background",
              "type": "background-pro",
              "status": "native",
              "source": "The tile fill"
            },
            {
              "group": "Preset · State: Default",
              "key": "icon_color",
              "type": "compact color",
              "status": "native",
              "source": "The centred glyph colour"
            },
            {
              "group": "Preset · State: Default",
              "key": "border_style",
              "type": "short-select",
              "status": "native",
              "source": "`solid` when the source tile has a border"
            },
            {
              "group": "Preset · State: Default",
              "key": "border_width",
              "type": "unit-input",
              "status": "native",
              "source": "Carried when the source tile has a border"
            },
            {
              "group": "Preset · State: Default",
              "key": "border_color",
              "type": "compact color",
              "status": "native",
              "source": "Carried when the source tile has a border"
            },
            {
              "group": "Preset · State: Default",
              "key": "box_shadow",
              "type": "box-shadow",
              "status": "unmapped",
              "source": "Left at the theme default"
            },
            {
              "group": "Preset · State: Hover",
              "key": "(all fields)",
              "type": "—",
              "status": "unmapped",
              "source": "Left empty → inherit Default"
            },
            {
              "group": "Preset · Shared (`shared_bottom`)",
              "key": "badge_shape",
              "type": "short-select",
              "status": "native",
              "source": "circle / rounded / square / hexagon — from the source tile"
            },
            {
              "group": "Preset · Shared (`shared_bottom`)",
              "key": "badge_size",
              "type": "unit-input",
              "status": "native",
              "source": "The tile’s width & height"
            },
            {
              "group": "Preset · Shared (`shared_bottom`)",
              "key": "icon_size",
              "type": "unit-input",
              "status": "native",
              "source": "Derived as ≈ 50% of the tile size"
            },
            {
              "group": "Preset · Shared (`shared_bottom`)",
              "key": "border_radius",
              "type": "unit-input",
              "status": "native",
              "source": "For the Rounded shape (Circle is always 50%)"
            },
            {
              "group": "Preset · Shared (`shared_bottom`)",
              "key": "transition",
              "type": "short-text (ms)",
              "status": "auto",
              "source": "Fixed at `200` ms"
            },
            {
              "group": "Preset · Shared (`shared_bottom`)",
              "key": "hover_fx",
              "type": "multi-select",
              "status": "unmapped",
              "source": "Not derived"
            },
            {
              "group": "Preset · Shared (`shared_bottom`)",
              "key": "custom_css",
              "type": "code-editor ({{SELECTOR}})",
              "status": "unmapped",
              "source": "Not derived"
            }
          ]
        },
        {
          "name": "Image Styles",
          "slug": "image-styles",
          "ref": "/theme/components/image-styles",
          "coverage": "populated",
          "intro": "Radius / circle / aspect / filter from the source’s images (`build_image_styles`) → reusable image treatments.",
          "options": [
            {
              "key": "radius",
              "type": "unit",
              "status": "native",
              "source": "Corner radius"
            },
            {
              "key": "circle",
              "type": "toggle",
              "status": "native",
              "source": "Fully-round crop when the source uses it"
            },
            {
              "key": "aspect ratio",
              "type": "ratio",
              "status": "native",
              "source": "Fixed aspect when detected"
            },
            {
              "key": "filter",
              "type": "css-filter",
              "status": "native",
              "source": "Grayscale / brightness etc. when present"
            }
          ]
        },
        {
          "name": "Section Styles",
          "slug": "section-styles",
          "ref": "/theme/components/section-styles",
          "coverage": "populated",
          "intro": "Reusable band skins clustered from the source’s sections (`build_section_style_presets`) — a repeated section look becomes one editable Section Style.",
          "options": [
            {
              "key": "band background",
              "type": "background",
              "status": "native",
              "source": "The section fill (colour / gradient)"
            },
            {
              "key": "padding",
              "type": "box",
              "status": "native",
              "source": "Vertical band padding"
            },
            {
              "key": "text colour",
              "type": "colour",
              "status": "native",
              "source": "On-band text colour (for dark bands)"
            }
          ]
        },
        {
          "name": "Container Widths",
          "slug": "container-widths",
          "ref": "",
          "coverage": "populated",
          "intro": "Named content-band widths gathered from the source (`build_container_width_presets`) — Narrow / Medium / Wide plus any distinct widths the site uses, so converted sections reference **shared named widths** instead of one-off numbers.",
          "options": [
            {
              "key": "Narrow / Medium / Wide",
              "type": "unit",
              "status": "native",
              "source": "The recurring content widths, named"
            },
            {
              "key": "distinct source widths",
              "type": "unit",
              "status": "native",
              "source": "Any extra widths the source uses, added as named presets"
            }
          ]
        },
        {
          "name": "Background Patterns",
          "slug": "background-patterns",
          "ref": "/theme/components/background-patterns",
          "coverage": "populated",
          "intro": "Captured SVG / gradient decorative tiles (`build_background_patterns`, the PHP parity of the JS `backgroundPatterns()`); the mapper injects the matching overlay onto its section.",
          "options": [
            {
              "key": "captured tile",
              "type": "svg / gradient",
              "status": "native",
              "source": "The decorative pattern lifted from the source"
            },
            {
              "key": "placement",
              "type": "overlay",
              "status": "auto",
              "source": "The mapper wires the pattern onto the section that used it"
            }
          ]
        },
        {
          "name": "Spacing",
          "slug": "spacing",
          "ref": "/theme/components/spacing",
          "coverage": "populated",
          "intro": "The source’s spacing steps → an editable scale (`build_spacing_scale`), so the converted site carries a real scale instead of the theme default.",
          "options": [
            {
              "key": "spacing steps",
              "type": "scale",
              "status": "native",
              "source": "The distinct spacing values the source uses"
            }
          ]
        },
        {
          "name": "Gaps",
          "slug": "gaps",
          "ref": "/theme/components/spacing",
          "coverage": "populated",
          "intro": "The source’s column / grid gutters → an editable gap scale (`build_gap_scale`) — mirrors the extended default and appends any off-scale gutter **exactly** (a `gap-[20px]` gets an exact entry, no snap).",
          "options": [
            {
              "key": "gap steps",
              "type": "scale",
              "status": "native",
              "source": "The default scale plus the source’s gutters"
            },
            {
              "key": "off-scale gutter",
              "type": "exact",
              "status": "native",
              "source": "Non-standard gutters kept exact (no snap-to-nearest)"
            }
          ]
        }
      ]
    },
    {
      "group": "General",
      "tab": "Typography & Fonts",
      "slug": "typography",
      "storageKey": "typography",
      "location": "Theme Settings → General → Typography",
      "coverage": "populated",
      "intro": "The Typography tab sets the site fonts and type scale. The converter derives the **heading font family** and writes it to `typography.heading_font`; the **body font** is enforced by the generated child theme’s scoped CSS.",
      "recognizerNotes": "The type **scale** the converter distils from the source headings and paragraphs is written to the shortcodes’ **[Text Styles](/theme/components/text-styles)** component (the Display / Lead / Small roles), **not** the per-heading overrides on this tab. Link colours and per-heading size overrides are left at the theme defaults.",
      "options": [
        {
          "key": "heading_font",
          "type": "typography",
          "status": "native",
          "source": "Heading font family, from the source headings → `typography.heading_font`"
        },
        {
          "key": "body",
          "type": "typography",
          "status": "via-css",
          "source": "Body font enforced by the generated child-theme CSS (`body { font-family … }`); the native family may stay “inherit”"
        },
        {
          "key": "h1–h6",
          "type": "typography",
          "status": "unmapped",
          "source": "Per-heading size overrides — the type scale goes to Components → Text Styles instead"
        },
        {
          "key": "body_link",
          "type": "compact color",
          "status": "unmapped",
          "source": "Default (uses the palette)"
        },
        {
          "key": "body_link_hover",
          "type": "compact color",
          "status": "unmapped",
          "source": "Default"
        },
        {
          "key": "body_link_underline",
          "type": "select",
          "status": "unmapped",
          "source": "Default (hover)"
        },
        {
          "key": "custom_fonts",
          "type": "addable-box",
          "status": "unmapped",
          "source": "Non-Google fonts are not registered here automatically"
        }
      ]
    },
    {
      "group": "General",
      "tab": "Backgrounds (Site / Section / Footer)",
      "slug": "backgrounds",
      "storageKey": "general_layout.site_background · footer_background",
      "location": "Theme Settings → General → Layout (Site Background) · per-Section · Footer",
      "coverage": "populated",
      "intro": "A background detector runs across the **body**, each **section**, and the **footer**, and writes the fills it finds — colour, gradient, photo, or video.",
      "recognizerNotes": "The body background is written **once** as a global Theme Setting (`general_layout.site_background`) so the whole site inherits it, and the theme-generator emits `body { background-color: var(--site-bg-color, …) }` **without** `!important` so a later edit still wins. Background **videos** are side-loaded to the Media Library so they stay editable in the Background → Video picker. Per-section band fills live on each **Section** element, not on a Theme Setting.",
      "options": [
        {
          "key": "general_layout.site_background",
          "type": "background-pro",
          "status": "native",
          "source": "The body background — colour / gradient / image, written once as a global setting"
        },
        {
          "key": "(section band fills)",
          "type": "background-pro",
          "status": "native",
          "source": "Per-section colour / gradient / photo / video — stored on each Section, not Theme Settings"
        },
        {
          "key": "footer_background",
          "type": "background-pro",
          "status": "native",
          "source": "The footer band’s background fill"
        },
        {
          "key": "footer_text_color",
          "type": "compact color",
          "status": "native",
          "source": "The footer’s muted text colour"
        },
        {
          "key": "site_background_pattern",
          "type": "multi-picker",
          "status": "unmapped",
          "source": "Not derived — set by hand"
        }
      ]
    },
    {
      "group": "General",
      "tab": "Layout",
      "slug": "layout",
      "storageKey": "general_layout",
      "location": "Theme Settings → General → Layout",
      "coverage": "partial",
      "intro": "The Layout tab controls the overall canvas — site width, container width, spacing density, roundness. The converter carries the **content container width** so body sections match the source; the rest is left for you to tune.",
      "recognizerNotes": "The source’s content max-width is written to `general_layout.layout_container_width` (the desktop value) so converted body sections finally line up with the source. Width mode, gutter, content density and roundness are left at the theme defaults.",
      "options": [
        {
          "key": "layout_container_width",
          "type": "responsive → unit-input",
          "status": "native",
          "source": "Source content max-width → the desktop container width"
        },
        {
          "key": "site_width_mode",
          "type": "multi-picker",
          "status": "unmapped",
          "source": "Default (full)"
        },
        {
          "key": "layout_container_gutter",
          "type": "unit-input",
          "status": "unmapped",
          "source": "Default"
        },
        {
          "key": "layout_section_spacing",
          "type": "radio",
          "status": "unmapped",
          "source": "Default (cozy)"
        },
        {
          "key": "layout_roundness",
          "type": "radio",
          "status": "unmapped",
          "source": "Default (subtle)"
        },
        {
          "key": "layout_prose_width",
          "type": "unit-input",
          "status": "unmapped",
          "source": "Default"
        },
        {
          "key": "site_background",
          "type": "background-pro",
          "status": "via-css",
          "source": "Written by the background detector — see the Backgrounds tab"
        }
      ]
    },
    {
      "group": "Footer",
      "tab": "Footer",
      "slug": "footer",
      "storageKey": "footer_background · footer_text_color",
      "location": "Theme Settings → Footer → Layout",
      "coverage": "partial",
      "intro": "The Footer tab styles the footer band. The converter derives the footer’s **background fill** and **muted text colour**; the structural chrome (columns, menus, widgets) comes from the generated child theme + captured menus.",
      "recognizerNotes": "The footer background and text colour come from `detect_footer_style()`. The footer’s rows / columns / menus are built by the generated child theme and the captured menus (see [Header & Footer Elements](/theme/header-footer-elements)), not these design keys.",
      "options": [
        {
          "key": "footer_background",
          "type": "background-pro",
          "status": "native",
          "source": "Footer band background fill (from `detect_footer_style`)"
        },
        {
          "key": "footer_text_color",
          "type": "compact color",
          "status": "native",
          "source": "Muted footer text colour"
        },
        {
          "key": "footer_link_color",
          "type": "compact color",
          "status": "unmapped",
          "source": "Default"
        },
        {
          "key": "footer_border_top",
          "type": "multi-inline",
          "status": "unmapped",
          "source": "Default"
        },
        {
          "key": "footer_padding_top",
          "type": "select",
          "status": "unmapped",
          "source": "Default"
        },
        {
          "key": "footer_padding_bottom",
          "type": "select",
          "status": "unmapped",
          "source": "Default"
        },
        {
          "key": "(columns / menus / widgets)",
          "type": "Footer Columns",
          "status": "unmapped",
          "source": "Built by the generated child theme + captured menus, not these keys"
        }
      ]
    }
  ],
  "otherTabs": [
    {
      "group": "General",
      "tab": "Base",
      "coverage": "manual",
      "note": "Selection / scrollbar / focus colours — opt-in, set by hand."
    },
    {
      "group": "General",
      "tab": "Settings · Pages · Sidebar · Social",
      "coverage": "manual",
      "note": "Behavioural / structural defaults — not derived from a source design."
    },
    {
      "group": "Header",
      "tab": "Header (Identity, Layout, Top/Bottom bar, Menu, Mega Menu…)",
      "coverage": "manual",
      "note": "The header chrome comes from the generated child theme + captured menus, not Theme Settings design keys."
    },
    {
      "group": "Content",
      "tab": "Blog · Single · Archives · Pages",
      "coverage": "manual",
      "note": "Post/archive layout defaults — not part of a page conversion."
    },
    {
      "group": "Misc",
      "tab": "Misc",
      "coverage": "manual",
      "note": "Miscellaneous toggles — set by hand."
    }
  ]
}
